#pragma once
#include <cstdint>

static inline constexpr float InputLowRangeVolts = -10.f;
static inline constexpr float InputHighRangeVolts = 10.f;

static inline constexpr float OutputMaxVolts = 10.f;
