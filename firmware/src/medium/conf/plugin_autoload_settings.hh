#pragma once

#include <array>
#include <cstdint>
#include <string>
#include <vector>

namespace MetaModule
{

struct PluginAutoloadSettings {
	std::vector<std::string> slug;
};

} // namespace MetaModule
