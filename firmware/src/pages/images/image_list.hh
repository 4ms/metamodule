#pragma once
#include "lvgl/lvgl.h"
#include <string_view>

extern "C" {
LV_IMG_DECLARE(enosc_artwork_240);
}

struct ModuleImages {

	static const lv_img_dsc_t *get_image_by_slug(const std::string_view slug) {
		// AUTOGENERATED:
		//  if (slug == "{SLUG}")
		//		return &{slug}_artwork_240;

		if (slug == "EnOsc")
			return &enosc_artwork_240;

		return nullptr;
	}
};
