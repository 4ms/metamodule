name: Build VCV Rack Plugin

on:
  workflow_dispatch:
  push:
    tags:
      - 'vcv-[0-9]+.[0-9]+.[0-9]+'
    branches:
      - main
      - '83-*'
    paths:
      - 'shared/**'
      - 'vcv/**'
      - '!**.md' # Skip docs
      - '!shared/svgextract' # Skip generator script (still run CI if generated files change)
      - '!shared/gen_minblep' # Skip generator script (still run CI if generated files change)
      - '!shared/tableGen' # Skip generator script (still run CI if generated files change)

env:
  rack-sdk-version: latest
  rack-plugin-toolchain-dir: /home/build/rack-plugin-toolchain

defaults:
  run:
    shell: bash

jobs:  
  build-lin-x64:
    name: lin-x64
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/qno/rack-plugin-toolchain-win-linux
      options: --user root
    strategy:
      fail-fast: false
    steps:
      - name: Always build linux
        run: echo "VCV plugin lin-x64 workflow triggered"
      # - uses: actions/checkout@v3
      #   with:
      #     submodules: recursive
      # - name: Build plugin
      #   run: |          
      #     export PLUGIN_DIR=$GITHUB_WORKSPACE/vcv
      #     pushd ${{ env.rack-plugin-toolchain-dir }}
      #     make plugin-build-lin-x64
      # - name: Upload artifact
      #   uses: actions/upload-artifact@v3
      #   with:
      #     path: ${{ env.rack-plugin-toolchain-dir }}/plugin-build
      #     name: lin-x64

  build-win-x64:
    if: startsWith(github.ref, 'refs/tags/')
    name: win-x64
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/qno/rack-plugin-toolchain-win-linux
      options: --user root
    strategy:
      fail-fast: false
    steps:
      - name: Build windows only when pushing a tag
        run: echo "VCV plugin win-x64 workflow triggered"
      # - uses: actions/checkout@v3
      #   with:
      #     submodules: recursive
      # - name: Build plugin
      #   run: |          
      #     export PLUGIN_DIR=$GITHUB_WORKSPACE/vcv
      #     pushd ${{ env.rack-plugin-toolchain-dir }}
      #     make plugin-build-win-x64
      # - name: Upload artifact
      #   uses: actions/upload-artifact@v3
      #   with:
      #     path: ${{ env.rack-plugin-toolchain-dir }}/plugin-build
      #     name: win-x64

  build-mac:
    if: startsWith(github.ref, 'refs/tags/')
    name: mac 
    runs-on: macos-12
    strategy:
      fail-fast: false
      matrix:
        platform: [x64, arm64]
    steps:
      - name: Build Mac only when pushing a tag
        run: echo "VCV plugin Mac workflow triggered"

      # - uses: actions/checkout@v3
      #   with:
      #     submodules: recursive
      # - name: Get Rack-SDK
      #   run: |
      #     pushd $HOME
      #     wget -O Rack-SDK.zip https://vcvrack.com/downloads/Rack-SDK-${{ env.rack-sdk-version }}-mac-${{ matrix.platform }}.zip
      #     unzip Rack-SDK.zip
      # - name: Build plugin
      #   run: |
      #     cd vcv
      #     CROSS_COMPILE_TARGET_x64=x86_64-apple-darwin
      #     CROSS_COMPILE_TARGET_arm64=arm64-apple-darwin
      #     export RACK_DIR=$HOME/Rack-SDK
      #     export CROSS_COMPILE=$CROSS_COMPILE_TARGET_${{ matrix.platform }}
      #     make dep
      #     make dist
      # - name: Upload artifact
      #   uses: actions/upload-artifact@v3
      #   with:
      #     path: vcv/dist/*.vcvplugin
      #     name: mac-${{ matrix.platform }}

  publish:
    name: Publish plugin
    runs-on: ubuntu-latest
    needs: [build-lin-x64, build-win-x64, build-mac]
    steps:
      - name: Publish only if all other builds passed
        run: echo "VCV plugin publish triggered"

#       - uses: actions/download-artifact@v3
#         with:
#           path: _artifacts
#       - uses: FranzDiebold/github-env-vars-action@v2
#       - uses: softprops/action-gh-release@v1
#         with:        
#           name: "Development Build: ${{ env.CI_REF_NAME }}"
#           body: "VCV plugin version as of tag ${{ env.CI_REF_NAME }}"
#           files: |          
#             _artifacts/**/*.vcvplugin
      
